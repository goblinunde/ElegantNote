name: Build LaTeX and Release

on:
  push:
    branches: [main]
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
      
      - name: 🔧 Compile LaTeX document
        uses: xu-cheng/latex-action@v3
        with:
          root_file: latex-tutorial.tex
          latexmk_use_xelatex: true
      
      - name: 📦 Upload PDF artifact
        uses: actions/upload-artifact@v4
        with:
          name: latex-tutorial-pdf
          path: latex-tutorial.pdf
          retention-days: 30
      
      - name: 🏷️ Create Release (on tag push)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            latex-tutorial.pdf
          body: |
            ## 📚 LaTeX 数学符号与语法快速参考教程
            
            自动构建的 PDF 文档，基于 ElegantNote 模板。
            
            ### 更新内容
            - 查看 [CHANGELOG](https://github.com/${{ github.repository }}/commits/${{ github.ref_name }}) 获取详细更改
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # 自动发布最新版本到 latest release
  update-latest:
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main'
    
    steps:
      - name: 📥 Download artifact
        uses: actions/download-artifact@v4
        with:
          name: latex-tutorial-pdf
      
      - name: 🔄 Update latest release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: "Latest Build"
          files: latex-tutorial.pdf
          body: |
            ## 🚀 最新自动构建版本
            
            此 Release 会在每次推送到 main 分支时自动更新。
            
            **构建时间**: ${{ github.event.head_commit.timestamp }}
            **提交**: ${{ github.sha }}
          prerelease: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
